[project]
name = "hyprland-scripts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "hyprpy>=0.2.0",
    "pygithub>=2.8.1",
    "python-dotenv>=1.2.1",
]

[dependency-groups]
dev = [
    "ipython>=9.9.0",
]
